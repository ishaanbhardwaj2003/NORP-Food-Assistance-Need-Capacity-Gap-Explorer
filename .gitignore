# OS / editor cruft
.DS_Store
*.DS_Store
~$*
*.code-workspace

# Local development notes (not part of the project deliverables)
CLAUDE.md
.claude/

# Local clone of the TA benchmark branch (nested git repo; the branch itself is
# fetched into this repo as ai-suggestions/cp4 for auditability)
TA-FEEDBACK-GOABOVEANDBEYOND/

# The single-file full NGO export (~162 MB) exceeds GitHub's 100 MB cap; the
# committed canonical form is the split parts in data/raw/ngos_full/.
data/raw/NGOs_with_categories.csv.gz

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.ipynb_checkpoints/

# NOTE: data/output/ is intentionally NOT ignored.
# The generated profiler_log.json and joined_county_panel.csv are committed to
# main as self-contained Supporting Evidence that the pipeline runs end-to-end
# (per Checkpoint 1 grading feedback).

# Local working documents (reports, feedback, planning notes) stay out of the repo
CP3_REVISION_NOTES.md
CS6365_Checkpoint1_Report.pdf
CS6365_Checkpoint2_Report.docx
CS6365_Checkpoint_Template.docx
feedback.txt
implementation_plan.md
read-the-implementation-plan-refactored-crown.md

# Presentation deck build script (the deck itself in presentation/ is committed)
scripts/make_slides.py
