pandas>=2.0
numpy>=1.24
matplotlib>=3.7
scipy>=1.10
anthropic>=0.40
# scipy powers the z-score based gap score in src/join_logic.py and the
# Pearson/Spearman grid in src/correlation_agent.py.
# anthropic is only exercised by `run_analysis.py --live`; the default offline
# mode replays the committed data/output/llm_candidates.json with no API key.
pytest>=8.0
# pytest runs the committed test suite in tests/ (no API key, no large files).
